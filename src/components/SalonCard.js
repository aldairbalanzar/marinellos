import React from 'react'

const SalonCard = () => {
    return (
        <div>
            {/* <p>{name ? name : "NAME"}</p>
            <p>{address ? address : 'LOCATION'}</p>
            <p>{number ? number : 'NUMBER'}</p> */}
            <p>NAME</p>
            <p>LOCATION</p>
            <p>NUMBER</p>
        </div>
    )
}

export default SalonCard
